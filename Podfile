# Uncomment this line to define a global platform for your project
platform :ios, '8.0'

use_frameworks!

target 'FBBugDemonstration' do
    pod 'TestPod', :git => "https://github.com/Rawrcoon/TestPod.git"
end
